# index.html Header Complete Dependencies

## 1. **HTML Structure** (Lines 48-150)
```
<header class="site-header">
  - .brand.brand-title (logo animation)
  - .robot-scene (plant/robot animation) 
  - #site-nav.site-nav (dropdown nav)
  - .nav-dropdown + .dropdown-menu (About Us/Business Verticals/Projects)
  - #mobile-menu-button (hamburger)
  - #mobile-nav (mobile menu)
</header>
```

## 2. **CSS Files** (styles.css - Key Classes)
```
.site-header (sticky, backdrop-blur)
.header-container (flex layout)
.brand / .brand-title (logo animation)
.robot-scene / .robot / .plant-container (animations)
.site-nav / .nav-dropdown / .dropdown-btn / .dropdown-menu
.mobile-menu-btn / #mobile-nav
.btn.btn-primary.header-cta
```

## 3. **JavaScript Files**
```
scripts.js (mobile menu toggle, dropdowns)
product-navigation-fixed.js (sticky nav)
```

## 4. **Fonts** (head section)
```
Inter:wght@400;500;600;700;800 (Google Fonts)
```

## 5. **Animation Keyframes** (styles.css)
```
@keyframes robotMove, eyeBlink, armReach
@keyframes seedDrop, stemGrow, leafGrow
@keyframes brandFade (logo text)
```

**Total:** 1 HTML block + styles.css + 2 JS files + Inter font = **COMPLETE HEADER SYSTEM**

**Apply to career.html:** Copy HTML block + link styles.css + scripts.js + product-navigation-fixed.js + Inter font

